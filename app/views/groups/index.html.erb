<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="text-center bg-blue ">
    <h1 class="font-bold text-4xl text-white">Transactions</h1>
  </div>

      <%= link_to "New group", new_user_group_path(current_user), class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>

  <div id="groups" class="m-10 bg-white ">
    <%= render @groups %>
  </div>
</div>
